{% extends 'base.html.twig' %}

{% block body %}
    <h1>Qg creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Creer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('qg_index') }}">revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
