{% extends 'base.html.twig' %}

{% block body %}
    <h1>Qg</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qg.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ qg.nom }}</td>
            </tr>
            <tr>
                <th>Desk(x)</th>
                <td>{{ qg.deskx | join(',') }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ qg.localisation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('qg_index') }}">Revenir à la liste</a>
        </li>
        <li>
            <a href="{{ path('qg_edit', { 'id': qg.id }) }}">Editer</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
