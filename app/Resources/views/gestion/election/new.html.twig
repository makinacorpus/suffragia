{% extends 'base.html.twig' %}

{% block body %}
    <h1>Election creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('election_index') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
