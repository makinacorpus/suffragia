{% extends 'base.html.twig' %}

{% block body %}
    <h1>Candidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ candidat.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ candidat.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('candidat_index') }}">Revenir à la liste</a>
        </li>
        <li>
            <a href="{{ path('candidat_edit', { 'id': candidat.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    <a href="{{ path('candidat_new') }}">new Candidate</a>
{% endblock %}
