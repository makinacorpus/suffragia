{% extends 'base.html.twig' %}

{% block body %}
    <h1>Desk creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('desk_index') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
