{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parti creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Creer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('parti_index') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
