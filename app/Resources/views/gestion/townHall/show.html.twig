{% extends 'base.html.twig' %}

{% block body %}
    <h1>TownHall</h1>

    <table  class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ townHall.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ townHall.nom }}</td>
            </tr>
            <tr>
                <th>QG</th>
                <td>{{ townHall.qgs | join (",")}}</td>
            </tr>
            <tr>
                <th>Desk(x)</th>
                <td>{{ deskx | join (",")}}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('townHall_index') }}">Revenir à la liste</a>
        </li>
        <li>
            <a href="{{ path('townHall_edit', { 'id': townHall.id }) }}">Editer</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
